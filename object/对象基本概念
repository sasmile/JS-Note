对象的两种定义方法：
 1.对象字面量：
    var myObj={
      a:2,
      key:value
      };
      
 2.构造形式：
    var myObj=new Object();
    myObj.key=value;
 通常使用对象字面量法；

对象中的一些概念：
  JS中的一切皆为对象的说法是错误的，JS中有许多特殊对象子类型，可以称之为复杂基本类型，函数就是对象的一个子类型，可以像操作其他对象一样操作函数；
  对象的属性值在引擎内部的存储方式多种多样，一般不会存储在对象容器内部，存储在对象容器内部的是属性名称，像指针一样指向值得真正存储位置；
  访问对象属性有两种方法，.操作符”属性访问“或[’‘]操作符”键访问“；
  对象中的属性名永远是字符串；

属性和方法：
  从技术角度讲，函数永远不”属于“一个对象，所以把对象内部引用的函数称为”方法”不合适；
  无论返回值是什么类型，每次访问对象的属性就是属性访问；
  最合适的说法是，函数和方法在JS中可以互换；
  即使是在对象中声明一个函数，这个函数也并不是属于这个对象，只是对相同函数对象的多个引用；
 
数组：
  如果向数组添加一个属性，属性名是数字，则这个属性名变成数组下标，因此会修改数组内容而不是添加属性；
  例：
   var myArray = ["foo",42];
   myArray["3"]="baz";
   myArray.length;//4
   myArray[3];//"baz"
  
